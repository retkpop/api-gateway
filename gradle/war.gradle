apply plugin: "war"

bootWar {
    mainClassName = "com.tk.api.gateway.ApiGatewayApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
